import type { Metadata } from "next";
export const metadata: Metadata={title:"DSH Market — DeepSeek Harness 插件市场",description:"发现、搜索并安装由社区构建的 DeepSeek Harness 插件。"};
export default function Layout({children}:{children:React.ReactNode}){return children}
